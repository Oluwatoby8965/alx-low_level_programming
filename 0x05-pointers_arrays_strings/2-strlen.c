#include "main.h"
#include <string.h>
/**
* _strlen -> function to get the length of a string
* @: string pointer to passed this function
* Return: returns length of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
